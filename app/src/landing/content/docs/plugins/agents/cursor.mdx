---
title: Cursor
description: Cursor Agent CLI — IDE-like editing from the terminal, with PATH-wrapper PR tracking.
---

import { Accordions, Accordion } from "fumadocs-ui/components/accordion";

<div style={{ display: "flex", alignItems: "center", gap: "0.75rem", margin: "0.5rem 0 1.25rem" }}>
	<Logo name="cursor" size={28} />
	<span style={{ fontSize: "0.8125rem", color: "var(--color-fd-muted-foreground)" }}>
		Slot: <code>agent</code> · Name: <code>cursor</code> · Binary: <code>agent</code>
	</span>
</div>

The [Cursor Agent CLI](https://cursor.com/docs) brings Cursor's editing model to a terminal. Thanos spawns it per session, wraps `gh`/`git`, and tracks activity via the Thanos activity log.

<PlatformSupport macos="full" linux="full" windows="full" />

## Install

```bash
curl https://cursor.com/install -fsS | bash
```

Verify:

```bash
agent --help
```

You should see "Cursor Agent" in the output. If you don't, the `agent` binary in your PATH is something else (e.g. an unrelated tool) — reorder your PATH so the Cursor one wins.

## Use

```yaml title="thanos.yaml"
agent: cursor
```

No plugin-level config.

## How it works

- **Launch:** Thanos runs `agent` inside the worktree.
- **Activity tracking:** Thanos writes a JSONL to `{workspace}/.thanos/activity.jsonl` each poll cycle based on terminal output classification. The dashboard reads from there.
- **PR + git tracking:** PATH wrappers at `~/.thanos/bin/gh` / `~/.thanos/bin/git` intercept calls to record PR metadata.
- **Session resume:** Not supported — every spawn is a fresh Cursor session.

## Detection

The plugin's `detect()` checks `agent --help` output for the strings `Cursor Agent` and `--approve-mcps` to avoid false positives with other binaries named `agent`.

## Environment variables

| Variable        | Set by Thanos | Purpose                    |
| --------------- | --------- | -------------------------- |
| `THANOS_SESSION_ID` | ✓         | Thanos session id              |
| `THANOS_ISSUE_ID`   | ✓         | Issue identifier           |
| `PATH`          | ✓         | Prepends `~/.thanos/bin`       |
| `GH_PATH`       | ✓         | Absolute path to real `gh` |

## Troubleshooting

<Accordions>
	<Accordion title="Thanos says Cursor Agent isn't detected">
		The plugin only recognises the real Cursor binary by its help output. Run `agent --help` — if you see a different
		tool's help, remove or shadow it, or reinstall Cursor Agent.
	</Accordion>
	<Accordion title="Dashboard never leaves `active`">
		Cursor doesn't emit its own session JSONL. Thanos classifies based on terminal output — if your terminal is being
		captured by another tool, the classification can drift. `to doctor` will flag this.
	</Accordion>
</Accordions>
