{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main Thanos window: enables event listen/emit (agent output/exit streams) and the other default core APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
